{% extends 'base.html.twig' %}

{% block title %}Cycle{% endblock %}

{% block body %}
    <div class="container m-5">
    <h1>Cycle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cycle.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cycle.nom }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex">
    <a href="{{ path('cycle_index') }}" class="btn btn-warning me-3">Retour à la liste</a>

    <a href="{{ path('cycle_edit', {'id': cycle.id}) }}" class="btn btn-warning">Modifier</a>
    </div>

    {{ include('cycle/_delete_form.html.twig') }}
    </div>
{% endblock %}
